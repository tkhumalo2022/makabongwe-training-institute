name: Refresh social preview

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/refresh-social-preview.yml"
      - "scripts/capture-social-preview.mjs"
      - "app/layout.tsx"

permissions:
  contents: write

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install Playwright
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium
      - name: Capture live homepage
        run: node scripts/capture-social-preview.mjs
      - name: Commit preview image
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/images/social-preview.png
          if git diff --cached --quiet; then
            echo "No preview change detected."
          else
            git commit -m "Refresh Makabongwe homepage social preview"
            git push
          fi
